# Never upload secrets
.env

# Ignore virtual environments
.venv/
venv/

# Ignore Python cache
__pycache__/

# Subscriber PII must never enter this repo — it is public.
# The daily workflow used to stage backend/*.json back into main.
backend/subscribers.json
